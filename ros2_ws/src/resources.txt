base stater cmd :ros2 launch robot_bringup combined_launch.xml
rosdep update && rosdep install --ignore-src --from-paths . -y
export LIBGL_ALWAYS_SOFTWARE=1
export GAZEBO_MODEL_PATH=$GAZEBO_MODEL_PATH:/ros2_ws/install/robotiq_description/share
export GAZEBO_RESOURCE_PATH=$GAZEBO_RESOURCE_PATH:/ros2_ws/install/robotiq_description/share

pip install --force-reinstall setuptools==58.2.0

export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp

test: ros2 launch ur_simulation_gazebo ur_sim_control.launch.py

 ros2 control load_controller --set-state active forward_velocity_controller
 ros2 control set_controller_state joint_trajectory_controller inactive

sudo apt update
sudo apt install ros-humble-tf-transformations
pip install --upgrade --ignore-installed transforms3d

 ros2 control switch_controllers   --deactivate joint_trajectory_controller   --activate forward_velocity_controller   --strict

apt-get update
apt-get install -y ros-${ROS_DISTRO}-rqt-image-view

ros2 run rqt_image_view rqt_image_view --ros-args -r image:=/ibvs/visualization
rqt --standalone rqt_plot
